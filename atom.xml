<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>脆皮小栈</title>
  
  
  <link href="https://tsuiqg.xyz/atom.xml" rel="self"/>
  
  <link href="https://tsuiqg.xyz/"/>
  <updated>2021-06-22T05:05:48.095Z</updated>
  <id>https://tsuiqg.xyz/</id>
  
  <author>
    <name>脆皮丶</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>How To Set Up a Firewall with UFW on Ubuntu 20.04</title>
    <link href="https://tsuiqg.xyz/How-To-Set-Up-a-Firewall-with-UFW-on-Ubuntu-20-04/"/>
    <id>https://tsuiqg.xyz/How-To-Set-Up-a-Firewall-with-UFW-on-Ubuntu-20-04/</id>
    <published>2021-06-21T21:36:13.000Z</published>
    <updated>2021-06-22T05:05:48.095Z</updated>
    
    <content type="html"><![CDATA[<p>Introduction<br>UFW, or Uncomplicated Firewall, is a simplified firewall management interface that hides the complexity of lower-level packet filtering technologies such as <code>iptables</code> and <code>nftables</code>. If you’re looking to get started securing your network, and you’re not sure which tool to use, UFW may be the right choice for you.</p><p>This tutorial will show you how to set up a firewall with UFW on Ubuntu 20.04.</p><span id="more"></span><h1 id="Prerequisites"><a href="#Prerequisites" class="headerlink" title="Prerequisites"></a>Prerequisites</h1><p>To follow this tutorial, you will need:</p><p>UFW is installed by default on Ubuntu. If it has been uninstalled for some reason, you can install it with <code>sudo apt install ufw</code>.</p><h1 id="Step-1-—-Using-IPv6-with-UFW-Optional"><a href="#Step-1-—-Using-IPv6-with-UFW-Optional" class="headerlink" title="Step 1 — Using IPv6 with UFW (Optional)"></a>Step 1 — Using IPv6 with UFW (Optional)</h1><p>This tutorial is written with IPv4 in mind, but will work for IPv6 as well as long as you enable it. If your Ubuntu server has IPv6 enabled, ensure that UFW is configured to support IPv6 so that it will manage firewall rules for IPv6 in addition to IPv4. To do this, open the UFW configuration with nano or your favorite editor.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo nano /etc/default/ufw</span><br></pre></td></tr></table></figure><p>Then make sure the value of IPV6 is yes. It should look like this:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">IPV6=yes</span><br></pre></td></tr></table></figure><p>Save and close the file. Now, when UFW is enabled, it will be configured to write both IPv4 and IPv6 firewall rules. However, before enabling UFW, we will want to ensure that your firewall is configured to allow you to connect via SSH. Let’s start with setting the default policies.</p><h1 id="Step-2-—-Setting-Up-Default-Policies"><a href="#Step-2-—-Setting-Up-Default-Policies" class="headerlink" title="Step 2 — Setting Up Default Policies"></a>Step 2 — Setting Up Default Policies</h1><p>If you’re just getting started with your firewall, the first rules to define are your default policies. These rules control how to handle traffic that does not explicitly match any other rules. By default, UFW is set to deny all incoming connections and allow all outgoing connections. This means anyone trying to reach your server would not be able to connect, while any application within the server would be able to reach the outside world.</p><p>Let’s set your UFW rules back to the defaults so we can be sure that you’ll be able to follow along with this tutorial. To set the defaults used by UFW, use these commands:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw default deny incoming</span><br><span class="line">sudo ufw default allow outgoing</span><br></pre></td></tr></table></figure><p>These commands set the defaults to deny incoming and allow outgoing connections. These firewall defaults alone might suffice for a personal computer, but servers typically need to respond to incoming requests from outside users. We’ll look into that next.</p><h1 id="Step-3-—-Allowing-SSH-Connections"><a href="#Step-3-—-Allowing-SSH-Connections" class="headerlink" title="Step 3 — Allowing SSH Connections"></a>Step 3 — Allowing SSH Connections</h1><p>If we enabled our UFW firewall now, it would deny all incoming connections. This means that we will need to create rules that explicitly allow legitimate incoming connections — SSH or HTTP connections, for example — if we want our server to respond to those types of requests. If you’re using a cloud server, you will probably want to allow incoming SSH connections so you can connect to and manage your server.</p><p>To configure your server to allow incoming SSH connections, you can use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow ssh</span><br></pre></td></tr></table></figure><p>This will create firewall rules that will allow all connections on port <code>22</code>, which is the port that the SSH daemon listens on by default. UFW knows what port <code>allow ssh</code> means because it’s listed as a service in the <code>/etc/services</code> file.</p><p>However, we can actually write the equivalent rule by specifying the port instead of the service name. For example, this command works the same as the one above:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow 22</span><br></pre></td></tr></table></figure><p>If you configured your SSH daemon to use a different port, you will have to specify the appropriate port. For example, if your SSH server is listening on port <code>2222</code>, you can use this command to allow connections on that port:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow 2222</span><br></pre></td></tr></table></figure><p>Now that your firewall is configured to allow incoming SSH connections, we can enable it.</p><h1 id="Step-4-—-Enabling-UFW"><a href="#Step-4-—-Enabling-UFW" class="headerlink" title="Step 4 — Enabling UFW"></a>Step 4 — Enabling UFW</h1><p>To enable UFW, use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw enable</span><br></pre></td></tr></table></figure><p>You will receive a warning that says the command may disrupt existing SSH connections. We already set up a firewall rule that allows SSH connections, so it should be fine to continue. Respond to the prompt with <code>y</code> and hit <code>ENTER</code>.</p><p>The firewall is now active. Run the <code>sudo ufw status verbose</code> command to see the rules that are set. The rest of this tutorial covers how to use UFW in more detail, like allowing or denying different kinds of connections.</p><h1 id="Step-5-—-Allowing-Other-Connections"><a href="#Step-5-—-Allowing-Other-Connections" class="headerlink" title="Step 5 — Allowing Other Connections"></a>Step 5 — Allowing Other Connections</h1><p>At this point, you should allow all of the other connections that your server needs to respond to. The connections that you should allow depends on your specific needs. Luckily, you already know how to write rules that allow connections based on a service name or port; we already did this for SSH on port <code>22</code>. You can also do this for:</p><ul><li>HTTP on port 80, which is what unencrypted web servers use, using <code>sudo ufw allow http</code> or <code>sudo ufw allow 80</code></li><li>HTTPS on port 443, which is what encrypted web servers use, using sudo ufw allow https or sudo ufw allow 443<br>There are several others ways to allow other connections, aside from specifying a port or known service.</li></ul><h2 id="Specific-Port-Ranges"><a href="#Specific-Port-Ranges" class="headerlink" title="Specific Port Ranges"></a>Specific Port Ranges</h2><p>You can specify port ranges with UFW. Some applications use multiple ports, instead of a single port.</p><p>For example, to allow X11 connections, which use ports <code>6000-6007</code>, use these commands:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow 6000:6007/tcp</span><br><span class="line">sudo ufw allow 6000:6007/udp</span><br></pre></td></tr></table></figure><p>When specifying port ranges with UFW, you must specify the protocol (<code>tcp</code> or <code>udp</code>) that the rules should apply to. We haven’t mentioned this before because not specifying the protocol automatically allows both protocols, which is OK in most cases.</p><h2 id="Specific-IP-Addresses"><a href="#Specific-IP-Addresses" class="headerlink" title="Specific IP Addresses"></a>Specific IP Addresses</h2><p>When working with UFW, you can also specify IP addresses. For example, if you want to allow connections from a specific IP address, such as a work or home IP address of <code>203.0.113.4</code>, you need to specify <code>from</code>, then the IP address:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow from 203.0.113.4</span><br></pre></td></tr></table></figure><p>You can also specify a specific port that the IP address is allowed to connect to by adding <code>to any port</code> followed by the port number. For example, If you want to allow <code>203.0.113.4</code> to connect to port <code>22</code> (SSH), use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow from 203.0.113.4 to any port 22</span><br></pre></td></tr></table></figure><h2 id="Subnets"><a href="#Subnets" class="headerlink" title="Subnets"></a>Subnets</h2><p>If you want to allow a subnet of IP addresses, you can do so using CIDR notation to specify a netmask. For example, if you want to allow all of the IP addresses ranging from <code>203.0.113.1</code> to <code>203.0.113.254</code> you could use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow from 203.0.113.0/24</span><br></pre></td></tr></table></figure><p>Likewise, you may also specify the destination port that the subnet <code>203.0.113.0/24</code> is allowed to connect to. Again, we’ll use port <code>22</code> (SSH) as an example:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow from 203.0.113.0/24 to any port 22</span><br></pre></td></tr></table></figure><h2 id="Connections-to-a-Specific-Network-Interface"><a href="#Connections-to-a-Specific-Network-Interface" class="headerlink" title="Connections to a Specific Network Interface"></a>Connections to a Specific Network Interface</h2><p>If you want to create a firewall rule that only applies to a specific network interface, you can do so by specifying “allow in on” followed by the name of the network interface.</p><p>You may want to look up your network interfaces before continuing. To do so, use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ip addr</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Output Excerpt</span><br><span class="line">2: eth0: &lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt; mtu 1500 qdisc pfifo_fast state</span><br><span class="line">. . .</span><br><span class="line">3: eth1: &lt;BROADCAST,MULTICAST&gt; mtu 1500 qdisc noop state DOWN group default</span><br><span class="line">. . .</span><br></pre></td></tr></table></figure><p>The highlighted output indicates the network interface names. They are typically named something like <code>eth0</code> or <code>enp3s2</code>.</p><p>So, if your server has a public network interface called <code>eth0</code>, you could allow HTTP traffic (port <code>80</code>) to it with this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow in on eth0 to any port 80</span><br></pre></td></tr></table></figure><p>Doing so would allow your server to receive HTTP requests from the public internet.</p><p>Or, if you want your MySQL database server (port <code>3306</code>) to listen for connections on the private network interface <code>eth1</code>, for example, you could use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow in on eth1 to any port 3306</span><br></pre></td></tr></table></figure><p>This would allow other servers on your private network to connect to your MySQL database.</p><h1 id="Step-6-—-Denying-Connections"><a href="#Step-6-—-Denying-Connections" class="headerlink" title="Step 6 — Denying Connections"></a>Step 6 — Denying Connections</h1><p>If you haven’t changed the default policy for incoming connections, UFW is configured to deny all incoming connections. Generally, this simplifies the process of creating a secure firewall policy by requiring you to create rules that explicitly allow specific ports and IP addresses through.</p><p>However, sometimes you will want to deny specific connections based on the source IP address or subnet, perhaps because you know that your server is being attacked from there. Also, if you want to change your default incoming policy to <strong>allow</strong> (which is not recommended), you would need to create <strong>deny</strong> rules for any services or IP addresses that you don’t want to allow connections for.</p><p>To write <strong>deny</strong> rules, you can use the commands described above, replacing <strong>allow</strong> with <strong>deny</strong>.</p><p>For example, to deny HTTP connections, you could use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw deny http</span><br></pre></td></tr></table></figure><p>Or if you want to deny all connections from <code>203.0.113.4</code> you could use this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw deny from 203.0.113.4</span><br></pre></td></tr></table></figure><p>Now let’s take a look at how to delete rules.</p><h1 id="Step-7-—-Deleting-Rules"><a href="#Step-7-—-Deleting-Rules" class="headerlink" title="Step 7 — Deleting Rules"></a>Step 7 — Deleting Rules</h1><p>Knowing how to delete firewall rules is just as important as knowing how to create them. There are two different ways to specify which rules to delete: by rule number or by the actual rule (similar to how the rules were specified when they were created). We’ll start with the <strong>delete by rule number</strong> method because it is easier.</p><h2 id="By-Rule-Number"><a href="#By-Rule-Number" class="headerlink" title="By Rule Number"></a>By Rule Number</h2><p>If you’re using the rule number to delete firewall rules, the first thing you’ll want to do is get a list of your firewall rules. The UFW status command has an option to display numbers next to each rule, as demonstrated here:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw status numbered</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Numbered Output:</span><br><span class="line">Status: active</span><br><span class="line"></span><br><span class="line">     To      Action      From</span><br><span class="line">     --      ------      ----</span><br><span class="line">[ 1] 22      ALLOW IN    15.15.15.0/24</span><br><span class="line">[ 2] 80      ALLOW IN    Anywhere</span><br></pre></td></tr></table></figure><p>If we decide that we want to delete rule 2, the one that allows port 80 (HTTP) connections, we can specify it in a UFW delete command like this:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw delete 2</span><br></pre></td></tr></table></figure><p>This would show a confirmation prompt then delete rule 2, which allows HTTP connections. Note that if you have IPv6 enabled, you would want to delete the corresponding IPv6 rule as well.</p><h2 id="By-Actual-Rule"><a href="#By-Actual-Rule" class="headerlink" title="By Actual Rule"></a>By Actual Rule</h2><p>The alternative to rule numbers is to specify the actual rule to delete. For example, if you want to remove the <code>allow http</code> rule, you could write it like this:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw delete allow http</span><br></pre></td></tr></table></figure><p>You could also specify the rule by <code>allow 80</code>, instead of by service name:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw delete allow 80</span><br></pre></td></tr></table></figure><p>This method will delete both IPv4 and IPv6 rules, if they exist.</p><h1 id="Step-8-—-Checking-UFW-Status-and-Rules"><a href="#Step-8-—-Checking-UFW-Status-and-Rules" class="headerlink" title="Step 8 — Checking UFW Status and Rules"></a>Step 8 — Checking UFW Status and Rules</h1><p>At any time, you can check the status of UFW with this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw status verbose</span><br></pre></td></tr></table></figure><p>If UFW is disabled, which it is by default, you’ll see something like this:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Output</span><br><span class="line">Status: inactive</span><br></pre></td></tr></table></figure><p>If UFW is active, which it should be if you followed Step 3, the output will say that it’s active and it will list any rules that are set. For example, if the firewall is set to allow SSH (port <code>22</code>) connections from anywhere, the output might look something like this:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Output</span><br><span class="line">Status: active</span><br><span class="line">Logging: on (low)</span><br><span class="line">Default: deny (incoming), allow (outgoing), disabled (routed)</span><br><span class="line">New profiles: skip</span><br><span class="line"></span><br><span class="line">To                         Action      From</span><br><span class="line">--                         ------      ----</span><br><span class="line">22/tcp                     ALLOW IN    Anywhere</span><br></pre></td></tr></table></figure><p>Use the <code>status</code> command if you want to check how UFW has configured the firewall.</p><h1 id="Step-9-—-Disabling-or-Resetting-UFW-optional"><a href="#Step-9-—-Disabling-or-Resetting-UFW-optional" class="headerlink" title="Step 9 — Disabling or Resetting UFW (optional)"></a>Step 9 — Disabling or Resetting UFW (optional)</h1><p>If you decide you don’t want to use UFW, you can disable it with this command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw disable</span><br></pre></td></tr></table></figure><p>Any rules that you created with UFW will no longer be active. You can always run <code>sudo ufw enable</code> if you need to activate it later.</p><p>If you already have UFW rules configured but you decide that you want to start over, you can use the reset command:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw reset</span><br></pre></td></tr></table></figure><p>This will disable UFW and delete any rules that were previously defined. Keep in mind that the default policies won’t change to their original settings, if you modified them at any point. This should give you a fresh start with UFW.</p><h1 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h1><p>Your firewall is now configured to allow (at least) SSH connections. Be sure to allow any other incoming connections that your server needs, while limiting any unnecessary connections, so your server will be functional and secure.</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Introduction&lt;br&gt;UFW, or Uncomplicated Firewall, is a simplified firewall management interface that hides the complexity of lower-level packet filtering technologies such as &lt;code&gt;iptables&lt;/code&gt; and &lt;code&gt;nftables&lt;/code&gt;. If you’re looking to get started securing your network, and you’re not sure which tool to use, UFW may be the right choice for you.&lt;/p&gt;
&lt;p&gt;This tutorial will show you how to set up a firewall with UFW on Ubuntu 20.04.&lt;/p&gt;</summary>
    
    
    
    
    <category term="Ubuntu" scheme="https://tsuiqg.xyz/tags/Ubuntu/"/>
    
    <category term="Firewall" scheme="https://tsuiqg.xyz/tags/Firewall/"/>
    
    <category term="Security" scheme="https://tsuiqg.xyz/tags/Security/"/>
    
  </entry>
  
</feed>
